<loadFolders>
{% for supported_version in cookiecutter.supported_versions.split(',') %}
  <v{{ supported_version }}>
    <li>{{ supported_version }}</li>
  </v{{ supported_version }}>
{% endfor %}
</loadFolders>